import 'package:flutter/material.dart';

class TapBarCity extends StatelessWidget {
  const TapBarCity({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 50,
      child:ListView.builder(
        
        itemBuilder: (context, index) {
        
      },) ,
    );
  }
}